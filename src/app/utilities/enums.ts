// Form Types
export enum FORM_TYPE {
    TEXT = "text",
    EMAIL = "email",
    PASSWORD = "password",
}

export enum BUTTON_VARIANT {
    PRIMARY = "primary",
}

// Slice Names
export enum SLICE_NAME {
    ACCOUNTS = "accounts",
    USERS = "users",
};