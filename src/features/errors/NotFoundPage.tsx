import { Container } from "react-bootstrap"

const NotFoundPage = () => {
  return (
    <Container>
      <h1>Opps - we've looked everywhere but could not find what you are looking for!</h1>
    </Container>
  )
}

export default NotFoundPage;